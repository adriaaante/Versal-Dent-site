# Generated single-file preview (built from index.html via /tmp/build_preview.py)
preview.html

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Telegram-конфиг — токен бота живёт только на сервере, не в репо.
# Создаётся один раз вручную из api/config.php.example
# (см. CLAUDE.md, раздел «Форма заявки»).
api/config.php
